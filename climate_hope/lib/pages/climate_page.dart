import 'package:flutter/material.dart';

class ClimatePage extends StatelessWidget {
  const ClimatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Become a Climate Champion!"));
  }
}
